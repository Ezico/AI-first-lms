export async function GET() {
  return new Response("Authentication is now handled by a custom system", { status: 200 })
}

export async function POST() {
  return new Response("Authentication is now handled by a custom system", { status: 200 })
}
